all:
  hosts:
    localhost:
      ansible_connection: local
      vars:
        # geerlingguy.docker
        docker_users:
          - michael
        # geerlingguy.pip
        pip_install_packages:
          - docker